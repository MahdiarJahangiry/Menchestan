namespace Menchestan
{
    namespace GamePlay
    {
        namespace Board
        {
            public enum PointType : byte
            {
                PiecePoint,
                WayPoint,
                SafePoint,
                EndSafePoint,
                StartPoint,
                EndPoint
            }
        }
    }
}
